class Parameters {
    companion object {
        const val MAX_ROUNDS = 10
        const val MAX_PIN_NUMBER = 10
        const val DEFAULT_BASE_SCORE = 0
        const val MAX_ROLL_PER_FRAME = 2
    }
}